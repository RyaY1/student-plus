#微服务配置
spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: student-plus-project
      config:
        namespace: dev
        group: student-plus-project
        file-extension: yaml
        refresh-enabled: true
  profiles:
    active: dev
logging:
  config: classpath:log4j2-dev.xml